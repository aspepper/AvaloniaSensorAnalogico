using Avalonia;
using Avalonia.Controls;

namespace SensorAnalogico.App.Views
{
    public partial class MainWindow : Window
    {
        public MainWindow(bool loadXaml = true, bool attachDevTools = true)
        {
            InitializeComponent();
        }
    }
}